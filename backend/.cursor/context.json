{
  "arch": {
    "stack": [
      "FastAPI",
      "Python 3.12",
      "OpenAI Agents SDK",
      "Scrapy",
      "Upstash Redis",
      "Render (deployment)",
      "GitHub Actions (CI/CD)"
    ],
    "decisions": [
      {
        "what": "Dynamic relevancy threshold",
        "why": "Filter keywords based on dataset size (2/3/5 threshold)",
        "when": "2025-10-24"
      },
      {
        "what": "Redis for job storage",
        "why": "Ephemeral file systems in deployed environments",
        "when": "2025-10-24"
      },
      {
        "what": "Batching for AI agents",
        "why": "Prevent API timeouts with large datasets (100-200 keywords per batch)",
        "when": "2025-10-24"
      },
      {
        "what": "Disabled Opportunity Agent",
        "why": "User requested to skip this scoring step",
        "when": "2025-10-24"
      },
      {
        "what": "Proxy rotation with 6 working proxies",
        "why": "Amazon blocking - added geographic diversity and rotation",
        "when": "2025-10-25"
      },
      {
        "what": "CI/CD with GitHub Actions + Render",
        "why": "Auto-deploy on push to main, run tests before deployment",
        "when": "2025-10-25"
      }
    ]
  },
  "features": {
    "dynamic_relevancy": {
      "files": ["backend/app/local_agents/research/runner.py"],
      "deps": [],
      "state": "active",
      "notes": "Adjusts min_relevancy_threshold: 2 (≤100 keywords), 3 (100-200), 5 (200+)"
    },
    "redis_job_storage": {
      "files": ["backend/app/services/job_manager.py"],
      "deps": ["upstash-redis"],
      "state": "active",
      "notes": "Persistent job storage with file-based fallback"
    },
    "ai_agent_batching": {
      "files": [
        "backend/app/local_agents/keyword/subagents/root_extraction_agent.py",
        "backend/app/local_agents/scoring/subagents/root_relevance_agent.py",
        "backend/app/local_agents/scoring/runner.py"
      ],
      "deps": [],
      "state": "active",
      "notes": "Batch sizes: 200 (root extraction), 100 (root relevance)"
    },
    "anti_blocking": {
      "files": [
        "backend/app/services/amazon/anti_blocking/middlewares.py",
        "backend/app/services/amazon/scraper.py"
      ],
      "deps": [],
      "state": "active",
      "notes": "User-Agent rotation, proxy rotation, smart retry, delay randomization"
    },
    "proxy_rotation": {
      "files": ["backend/app/services/amazon/anti_blocking/middlewares.py"],
      "deps": [],
      "state": "active",
      "notes": "6 working proxies (DE, BR, IT, RU, IN, SG), random rotation strategy"
    },
    "ci_cd_pipeline": {
      "files": [
        "backend/.github/workflows/test.yml",
        "backend/.github/workflows/deploy-notification.yml",
        "backend/render.yaml",
        "backend/Dockerfile",
        "backend/tests/"
      ],
      "deps": ["pytest", "pytest-cov", "ruff", "mypy"],
      "state": "active",
      "notes": "Auto-deploy on push to main after tests pass"
    }
  },
  "debt": [
    "Free proxies unreliable - consider paid proxy service for production"
  ],
  "env": {
    "OPENAI_API_KEY": "OpenAI API access",
    "UPSTASH_REDIS_URL": "Redis connection URL",
    "UPSTASH_REDIS_TOKEN": "Redis authentication",
    "SCRAPER_PROXY_LIST": "Comma-separated proxy list (6 working proxies)",
    "SCRAPER_RETRY_TIMES": "Max retry attempts (7)",
    "RANDOM_DELAY_MIN": "Min delay between requests (5.0s)",
    "RANDOM_DELAY_MAX": "Max delay between requests (10.0s)"
  }
}
